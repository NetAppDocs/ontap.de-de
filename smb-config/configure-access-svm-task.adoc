---
permalink: smb-config/configure-access-svm-task.html 
sidebar: sidebar 
keywords: svm, configure, smb, client, access, create, configure, exist, configuring, root, volume, lif, host-name, resolution, kerberos, configure smb access to an svm 
summary: Falls Sie noch keine SVM für den SMB-Clientzugriff konfiguriert haben, müssen Sie entweder eine neue SVM erstellen und konfigurieren oder eine vorhandene SVM konfigurieren. Die Konfiguration von SMB umfasst das Öffnen des SVM-Root-Volume-Zugriffs, das Erstellen eines SMB-Servers, das Erstellen einer LIF, das Aktivieren der Host-Name-Auflösung, das Konfigurieren der Namensdienste und, falls gewünscht, das Aktivieren der Kerberos-Sicherheit. 
---
= Konfiguration des SMB-Zugriffs auf ONTAP SVMs
:allow-uri-read: 
:icons: font
:imagesdir: ../media/


[role="lead"]
Falls Sie noch keine SVM für den SMB-Clientzugriff konfiguriert haben, müssen Sie entweder eine neue SVM erstellen und konfigurieren oder eine vorhandene SVM konfigurieren. Die Konfiguration von SMB umfasst das Öffnen des SVM-Root-Volume-Zugriffs, das Erstellen eines SMB-Servers, das Erstellen einer LIF, das Aktivieren der Host-Name-Auflösung, das Konfigurieren der Namensdienste und, falls gewünscht, das Aktivieren der Kerberos-Sicherheit.
