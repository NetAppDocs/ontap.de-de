---
permalink: smb-config/commands-manage-ntp-servers-reference.html 
sidebar: sidebar 
keywords: ontap, network, time, protocol, ntp, version, support, ntpv3, symmetric, authentication, sha, security, commands for managing symmetric authentication on ntp servers 
summary: 'Ab ONTAP 9.5 wird das Network Time Protocol (NTP) Version 3 unterstützt. NTPv3 beinhaltet eine symmetrische Authentifizierung mit SHA-1-Schlüsseln, was die Netzwerksicherheit erhöht.' 
---
= ONTAP Befehle zur Verwaltung der symmetrischen Authentifizierung auf NTP-Servern
:allow-uri-read: 
:icons: font
:imagesdir: ../media/


[role="lead"]
Ab ONTAP 9.5 wird das Network Time Protocol (NTP) Version 3 unterstützt. NTPv3 beinhaltet eine symmetrische Authentifizierung mit SHA-1-Schlüsseln, was die Netzwerksicherheit erhöht.

|===
| Dazu ... | Verwenden Sie diesen Befehl... 


 a| 
Konfiguration eines NTP-Servers ohne symmetrische Authentifizierung
 a| 
`cluster time-service ntp server create` `-server` `server_name`



 a| 
Einen NTP-Server mit symmetrischer Authentifizierung konfigurieren
 a| 
`cluster time-service ntp server create` `-server` `server_ip_address` `-key-id` `key_id`



 a| 
Symmetrische Authentifizierung für einen bestehenden NTP serverAn Server aktivieren. Ein bestehender NTP Server kann modifiziert werden, um die Authentifizierung durch Hinzufügen der erforderlichen Schlüssel-ID zu ermöglichen.
 a| 
`cluster time-service ntp server modify` `-server` `server_name` `-key-id` `key_id`



 a| 
Einen gemeinsamen NTP-Schlüssel konfigurieren
 a| 
`cluster time-service ntp key create` `-id` `shared_key_id` `-type` `shared_key_type` `-value` `shared_key_value`

[NOTE]
====
Gemeinsame Schlüssel werden über eine ID referenziert. Die ID, ihr Typ und ihr Wert müssen sowohl auf dem Knoten als auch auf dem NTP Server identisch sein.

====


 a| 
Einen NTP-Server mit einer unbekannten Schlüssel-ID konfigurieren
 a| 
`cluster time-service ntp server create` `-server` `server_name` `-key-id` `key_id`



 a| 
Ein Server mit einer Schlüssel-ID, die auf dem NTP-Server nicht konfiguriert ist, wird konfiguriert.
 a| 
`cluster time-service ntp server create` `-server` `server_name` `-key-id` `key_id`

[NOTE]
====
Die Schlüssel-ID, der Schlüsseltyp und der Schlüsselwert müssen mit der auf dem NTP-Server konfigurierten Schlüssel-ID, dem Schlüsseltyp und dem Schlüsselwert identisch sein.

====


 a| 
Symmetrische Authentifizierung deaktivieren
 a| 
`cluster time-service ntp server modify` `-server` `server_name` `-authentication disabled`

|===
.Verwandte Informationen
* link:https://docs.netapp.com/us-en/ontap-cli/search.html?q=cluster+time-service+ntp["Cluster Zeitdienst NTP"^]

