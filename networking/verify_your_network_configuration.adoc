---
sidebar: sidebar 
permalink: networking/verify_your_network_configuration.html 
keywords: verify, upgrade, considerations, network, configuration, upgrading, physical, wire, wiring, cable, cabling, layer 2, reachability, port, results, verify your networking configuration after upgrading to ontap 9.8 or later, network port reachability show -detail, repair reachability decision tree, layer 2 reachability, assigned broadcast domain, reachability-status, unexpected ports, unavailable ports, reachability repair 
summary: Überprüfen Sie Ihre Netzwerkkonfiguration nach dem Upgrade auf ONTAP 9.8 oder höher. 
---
= Nach einem Upgrade sollte die ONTAP Netzwerkkonfiguration überprüft werden.
:hardbreaks:
:allow-uri-read: 
:nofooter: 
:icons: font
:linkattrs: 
:imagesdir: ../media/


[role="lead"]
Nach dem Upgrade von ONTAP 9.7x oder älter auf ONTAP 9.8 oder neuer sollte die Netzwerkkonfiguration überprüft werden. Nach dem Upgrade überwacht ONTAP automatisch die Erreichbarkeit von Layer 2.

.Schritt
. Es wird überprüft, ob jeder Port die erwartete Broadcast-Domäne erreichen kann:
+
[source, cli]
----
network port reachability show -detail
----
+
Weitere Informationen zu  `network port reachability show` finden sich in der link:https://docs.netapp.com/us-en/ontap-cli/network-port-reachability-show.html["ONTAP-Befehlsreferenz"^].



Die Befehlsausgabe enthält die Ergebnisse der Erreichbarkeitsprüfung. Die folgende Entscheidungsstruktur und Tabelle bieten eine Übersicht über die Ergebnisse (Erreichbarkeitsstatus) und ermöglichen eine Einschätzung, ob und welche weiteren Schritte erforderlich sind.

image:ontap_nm_image1.png["Entscheidungsbaum zur Reparaturerreichbarkeit"]

[cols="20,80"]
|===


| Erreichbarkeitsstatus | Beschreibung 


 a| 
OK
 a| 
Der Port verfügt über Layer-2-Erreichbarkeit zu seiner zugewiesenen Broadcast-Domäne.

Wenn der Erreichbarkeitsstatus „ok“ ist, aber „unerwartete Ports“ vorhanden sind, kann das Zusammenführen einer oder mehrerer Broadcast-Domänen in Betracht gezogen werden. Weitere Informationen finden sich unter link:merge_broadcast_domains.html["Zusammenführen von Broadcast-Domänen"].

Wenn der Erreichbarkeitsstatus „ok“ ist, aber „nicht erreichbare Ports“ vorhanden sind, kann das Aufteilen einer oder mehrerer Broadcast-Domänen in Erwägung gezogen werden. Weitere Informationen finden sich unter link:split_broadcast_domains.html["Aufgeteilte Broadcast-Domänen"].

Wenn der Erreichbarkeitsstatus "ok" ist und keine unerwarteten oder nicht erreichbaren Ports vorhanden sind, ist Ihre Konfiguration korrekt.



 a| 
Fehlkonfigurierte Erreichbarkeit
 a| 
Der Port hat keine Layer-2-Erreichbarkeit zu seiner zugewiesenen Broadcast-Domäne, jedoch hat der Port Layer-2-Erreichbarkeit zu einer anderen Broadcast-Domäne.

Die Port-Erreichbarkeit kann wiederhergestellt werden. Bei Ausführung des folgenden Befehls weist das System den Port der Broadcast-Domäne zu, zu der er erreichbar ist:

`network port reachability repair -node -port`

Weitere Informationen finden sich unter link:repair_port_reachability.html["Reparatur der Port-Erreichbarkeit"].

Weitere Informationen zu  `network port reachability repair` finden sich in der link:https://docs.netapp.com/us-en/ontap-cli/network-port-reachability-repair.html["ONTAP-Befehlsreferenz"^].



 a| 
Unerreichbarkeit
 a| 
Der Port hat keine Layer-2-Erreichbarkeit zu einer bestehenden Broadcast-Domäne.

Die Port-Erreichbarkeit kann wiederhergestellt werden. Beim Ausführen des folgenden Befehls weist das System den Port einer neu automatisch erstellten Broadcast-Domäne im Default-IPspace zu:

`network port reachability repair -node -port`

Weitere Informationen finden sich unter link:repair_port_reachability.html["Reparatur der Port-Erreichbarkeit"].



 a| 
Erreichbarkeit über mehrere Domänen hinweg
 a| 
Der Port ist über Layer 2 zu seiner zugewiesenen Broadcast-Domäne erreichbar; er ist jedoch auch über Layer 2 zu mindestens einer weiteren Broadcast-Domäne erreichbar.

Die physische Konnektivität und die Switch-Konfiguration sollten überprüft werden, um festzustellen, ob sie fehlerhaft sind oder ob die dem Port zugewiesene Broadcast-Domäne mit einer oder mehreren Broadcast-Domänen zusammengeführt werden muss.

Weitere Informationen finden Sie unter link:merge_broadcast_domains.html["Zusammenführen von Broadcast-Domänen"] oder link:repair_port_reachability.html["Reparatur der Port-Erreichbarkeit"].



 a| 
unbekannt
 a| 
Wenn der Erreichbarkeitsstatus „unbekannt“ ist, einige Minuten abwarten und den Befehl erneut ausführen.

|===
Nach der Reparatur eines Ports müssen verschobene LIFs und VLANs überprüft und behoben werden. Wenn der Port Teil einer Schnittstellengruppe war, ist es außerdem erforderlich, nachzuvollziehen, was mit dieser Schnittstellengruppe geschehen ist. Weitere Informationen finden sich unter link:repair_port_reachability.html["Reparatur der Port-Erreichbarkeit"].
