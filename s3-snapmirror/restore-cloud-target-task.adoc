---
sidebar: sidebar 
permalink: s3-snapmirror/restore-cloud-target-task.html 
keywords: restore a bucket from a cloud target, restore, bucket, restore bucket, source bucket data loss, restore from destination bucket, bucket, snapmirror s3 data loss, snapmirror s3 restore bucket, snapmirror s3, snapmirror s3 restore bucket from cloud 
summary: Wenn Daten in einem Quell-Bucket verloren gehen oder beschädigt werden, lässt sich der Datenbestand durch Wiederherstellung aus einem Ziel-Bucket wieder auffüllen. 
---
= Wiederherstellung eines ONTAP S3-Buckets aus einem Cloud-Ziel
:hardbreaks:
:toclevels: 1
:allow-uri-read: 
:toclevels: 1
:nofooter: 
:icons: font
:linkattrs: 
:imagesdir: ../media/


[role="lead"]
Wenn Daten in einem Quell-Bucket verloren gehen oder beschädigt werden, lässt sich der Datenbestand durch Wiederherstellung aus einem Ziel-Bucket wieder auffüllen.

.Über diese Aufgabe
Sie können den Ziel-Bucket in einem vorhandenen Bucket oder in einem neuen Bucket wiederherstellen. Der Ziel-Bucket für die Wiederherstellung muss größer sein als der logisch genutzte Speicherplatz des Ziel-Buckets.

Wenn Sie einen vorhandenen Bucket verwenden, muss dieser beim Start einer Wiederherstellungsoperation leer sein. Die Wiederherstellung führt kein „Rollback“ eines Buckets in der Zeit durch, sondern füllt einen leeren Bucket mit seinem vorherigen Inhalt.

[role="tabbed-block"]
====
.System Manager
--
Die Sicherungsdaten wiederherstellen:

. *Schutz > Beziehungen* auswählen und dann *SnapMirror S3* wählen.
. Klicken Sie image:icon_kabob.gif["Symbol für Menüoptionen"] und wählen Sie dann *Wiederherstellen*.
. Unter *Quelle* *Vorhandener Bucket* (Standardeinstellung) oder *Neuer Bucket* auswählen.
+
** Um auf einen *vorhandenen Bucket* (Standardeinstellung) wiederherzustellen, sind folgende Schritte erforderlich:
+
*** Den Cluster und die Storage-VM auswählen, um nach dem vorhandenen Bucket zu suchen.
*** Wählen Sie den vorhandenen Bucket aus.
*** Kopieren und fügen Sie den Inhalt des _destination_ S3-Server-CA-Zertifikats ein.


** Für die Wiederherstellung in einem *neuen Bucket* sind die folgenden Werte anzugeben:
+
*** Der Cluster und die Storage-VM zum Hosten des neuen Buckets.
*** Name, Kapazität und Leistungsservicelevel des neuen Buckets. Siehe link:../s3-config/storage-service-definitions-reference.html["Storage-Servicelevel"] für weitere Informationen.
*** Der Inhalt des CA-Zertifikats des Ziel-S3-Servers.




. Unter *Ziel* den Inhalt des CA-Zertifikats des _source_ S3-Servers kopieren und einfügen.
. *Schutz > Beziehungen* kann zur Überwachung des Wiederherstellungsfortschritts verwendet werden.


--
.CLI Prozedur
--
. Den neuen Ziel-Bucket für die Wiederherstellung erstellen. Weitere Informationen finden sich unter link:create-cloud-backup-new-bucket-task.html["Erstellen einer Backup-Beziehung für einen Bucket (Cloud-Ziel)"].
. Einen Wiederherstellungsvorgang für den Ziel-Bucket initiieren:
`snapmirror restore -source-path _object_store_name_:/objstore -destination-path _svm_name_:/bucket/_bucket_name_`
+
.Beispiel
Das folgende Beispiel stellt einen Ziel-Bucket in einem bestehenden Bucket wieder her.
`clusterA::> snapmirror restore -source-path sgws.store:/objstore -destination-path vs0:/bucket/test-bucket`

+
Weitere Informationen zu  `snapmirror restore` finden sich in der link:https://docs.netapp.com/us-en/ontap-cli/snapmirror-restore.html["ONTAP-Befehlsreferenz"^].



--
====