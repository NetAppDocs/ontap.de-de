---
permalink: nfs-config/set-export-rules-index-number-task.html 
sidebar: sidebar 
keywords: manage, processing, order, export rules 
summary: 'Mit dem Befehl `vserver export-policy rule setindex` kann die Indexnummer einer bestehenden Exportregel manuell festgelegt werden. Dies ermöglicht die Angabe der Reihenfolge, in der ONTAP Exportregeln auf Clientanfragen anwendet.' 
---
= Verwalten der Verarbeitungsreihenfolge von ONTAP NFS Exportregeln
:allow-uri-read: 
:icons: font
:imagesdir: ../media/


[role="lead"]
Mit dem  `vserver export-policy rule setindex` Befehl lässt sich die Indexnummer einer bestehenden Exportregel manuell festlegen. Dies ermöglicht die Angabe der Reihenfolge, in der ONTAP Exportregeln auf Clientanfragen anwendet.

.Über diese Aufgabe
Falls die neue Indexnummer bereits verwendet wird, fügt der Befehl die Regel an der angegebenen Stelle ein und ordnet die Liste entsprechend neu.

.Schritt
. Die Indexnummer einer bestimmten Exportregel ändern:
+
`vserver export-policy rule setindex -vserver _virtual_server_name_ -policyname _policy_name_ -ruleindex _integer_ -newruleindex _integer_`



.Beispiel
Der folgende Befehl ändert die Indexnummer einer Exportregel mit der Indexnummer 3 auf die Indexnummer 2 in einer Exportregel namens rs1 auf der SVM namens vs1:

[listing]
----
vs1::> vserver export-policy rule setindex -vserver vs1
-policyname rs1 -ruleindex 3 -newruleindex 2
----