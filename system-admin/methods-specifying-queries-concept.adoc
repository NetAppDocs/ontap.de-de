---
permalink: system-admin/methods-specifying-queries-concept.html 
sidebar: sidebar 
keywords: method, use, query, operator, interface, management, unix, patterns, wildcards, command, parameter, argument 
summary: Die Managementoberfläche unterstützt Abfragen sowie UNIX-typische Muster und Platzhalter, sodass mehrere Werte in Befehlsparameterargumenten abgeglichen werden können. 
---
= Abfrageoperatoren in der ONTAP CLI verwenden
:allow-uri-read: 
:icons: font
:imagesdir: ../media/


[role="lead"]
Die Managementoberfläche unterstützt Abfragen sowie UNIX-typische Muster und Platzhalter, sodass mehrere Werte in Befehlsparameterargumenten abgeglichen werden können.

Die folgende Tabelle beschreibt die unterstützten Abfrageoperatoren:

[cols="10,90"]
|===
| Operator | Beschreibung 


 a| 
*
 a| 
Platzhalter, der mit allen Einträgen übereinstimmt.

Beispielsweise zeigt der Befehl `volume show -volume \*tmp*`eine Liste aller Volumes an, deren Namen die Zeichenkette `tmp`enthalten.



 a| 
!
 a| 
NICHT-Operator.

Gibt einen Wert an, der nicht abgeglichen werden soll; beispielsweise  `*!vs0*` gibt an, dass der Wert  `vs0` nicht abgeglichen werden soll.



 a| 
|
 a| 
ODER Operator.

Trennt zwei Werte, die verglichen werden sollen; zum Beispiel  `*vs0 | vs2*` passt entweder vs0 oder vs2. Mehrere ODER-Anweisungen können angegeben werden; zum Beispiel  `a | b* | \*c*` passt auf den Eintrag  `a`, auf jeden Eintrag, der mit  `b` beginnt, und auf jeden Eintrag, der  `c` enthält.



 a| 
..
 a| 
Bereichsoperator.

Beispielsweise  `*5..10*` entspricht jeder Wert von  `5` bis  `10` einschließlich.



 a| 
<
 a| 
Kleiner-als-Operator.

Beispielsweise entspricht  `*<20*` jeder Wert, der kleiner als  `20` ist.



 a| 
>
 a| 
Größer-als-Operator.

Beispielsweise `*>5*` entspricht jeder Wert, der größer als `5` ist.



 a| 
\<=
 a| 
Kleiner-gleich-Operator.

Beispielsweise entspricht  `*<=5*` jeder Wert, der kleiner oder gleich  `5` ist.



 a| 
>=
 a| 
Größer-oder-gleich-Operator.

Beispielsweise entspricht  `*>=5*` jeder Wert, der größer oder gleich  `5` ist.



 a| 
{`query`}
 a| 
Erweiterte Abfrage.

Eine erweiterte Abfrage muss als erstes Argument nach dem Befehlsnamen, vor allen anderen Parametern, angegeben werden.

Beispielsweise setzt der Befehl `volume modify {-volume \*tmp*} -state offline`alle Volumes, deren Namen die Zeichenkette `tmp`enthalten, offline.

|===
Wenn Sie Abfragezeichen als Literale parsen möchten, müssen Sie die Zeichen in doppelte Anführungszeichen setzen (zum Beispiel  `"<10"`, `"0..100"`, `"\*abc*"`, oder  `"a|b"`), damit die korrekten Ergebnisse zurückgegeben werden.

Dateinamen müssen in doppelte Anführungszeichen gesetzt werden, um die Interpretation von Sonderzeichen zu verhindern. Dies gilt auch für Sonderzeichen, die von der clustershell verwendet werden.

Sie können mehrere Abfrageoperatoren in einer Befehlszeile verwenden. Zum Beispiel zeigt der Befehl `volume show -size >1GB -percent-used <50 -vserver !vs1` alle Volumes an, die größer als 1 GB sind, zu weniger als 50 % belegt sind und sich nicht in der Storage Virtual Machine (SVM) mit dem Namen "`vs1`" befinden.

.Verwandte Informationen
link:../system-admin/keyboard-shortcuts-edit-cli-commands-reference.html["Tastenkombinationen zum Bearbeiten von CLI-Befehlen"]
