---
permalink: system-admin/autosupport-creates-sends-event-messages-concept.html 
sidebar: sidebar 
keywords: autosupport, event, triggered, messages, ems, data, example 
summary: AutoSupport erstellt ereignisgesteuerte AutoSupport-Meldungen, wenn das EMS ein Auslöseereignis verarbeitet. Eine ereignisgesteuerte AutoSupport-Meldung weist die Empfänger auf Probleme hin, die eine Korrekturmaßnahme erfordern, und enthält nur Informationen, die für das Problem relevant sind. Es lässt sich anpassen, welche Inhalte enthalten sind und wer die Meldungen erhält. 
---
= Informationen zu ONTAP AutoSupport ereignisgesteuerten Nachrichten
:allow-uri-read: 
:icons: font
:imagesdir: ../media/


[role="lead"]
AutoSupport erstellt ereignisgesteuerte AutoSupport-Meldungen, wenn das EMS ein Auslöseereignis verarbeitet. Eine ereignisgesteuerte AutoSupport-Meldung weist die Empfänger auf Probleme hin, die eine Korrekturmaßnahme erfordern, und enthält nur Informationen, die für das Problem relevant sind. Es lässt sich anpassen, welche Inhalte enthalten sind und wer die Meldungen erhält.

AutoSupport verwendet den folgenden Prozess, um ereignisgesteuerte AutoSupport-Nachrichten zu erstellen und zu versenden:

. Wenn das EMS ein Auslöseereignis verarbeitet, sendet das EMS AutoSupport eine Anfrage.
+
Ein Trigger-Ereignis ist ein EMS-Ereignis mit einem AutoSupport-Ziel und einem Namen, der mit einem  `callhome.` Präfix beginnt.

. AutoSupport erstellt eine ereignisgesteuerte AutoSupport-Nachricht.
+
AutoSupport sammelt grundlegende und zur Fehlerbehebung dienende Informationen aus Subsystemen, die mit dem Auslöser verknüpft sind, um eine Meldung zu erstellen, die nur Informationen enthält, die für das Auslöserereignis relevant sind.

+
Jedem Trigger ist standardmäßig ein Satz von Subsystemen zugeordnet. Es besteht jedoch die Möglichkeit, mithilfe des  `system node autosupport trigger modify` Befehls zusätzliche Subsysteme mit einem Trigger zu verknüpfen.

. AutoSupport sendet die ereignisgesteuerte AutoSupport-Nachricht an die Empfänger, die durch den  `system node autosupport modify` Befehl mit den  `-to`,  `-noteto`,  `-partner-address` und  `-support` Parametern definiert sind.
+
Die Zustellung von AutoSupport-Nachrichten für bestimmte Auslöser kann durch Verwendung des  `system node autosupport trigger modify` Befehls mit den  `-to` und  `-noteto` Parametern aktiviert oder deaktiviert werden.



.Beispiel für Daten, die für ein bestimmtes Ereignis gesendet werden
Das  `storage shelf PSU failed`EMS-Ereignis löst eine Meldung aus, die grundlegende Daten aus den Subsystemen Mandatory, Log Files, Storage, RAID, HA, Platform und Networking sowie Fehlerbehebungsdaten aus den Subsystemen Mandatory, Log Files und Storage enthält.

Es wird festgelegt, dass Daten über NFS in alle AutoSupport-Nachrichten aufgenommen werden, die als Reaktion auf ein zukünftiges  `storage shelf PSU failed` Ereignis gesendet werden. Der folgende Befehl wird eingegeben, um Daten zur Fehlerbehebung für NFS für das  `callhome.shlf.ps.fault` Ereignis zu aktivieren:

[listing]
----

        cluster1::\>
        system node autosupport trigger modify -node node1 -autosupport-message shlf.ps.fault -troubleshooting-additional nfs
----
Beachten Sie, dass das  `callhome.` Präfix beim  `callhome.shlf.ps.fault` Ereignis entfällt, wenn die  `system node autosupport trigger` Befehle verwendet werden oder wenn durch AutoSupport und EMS-Ereignisse in der CLI darauf verwiesen wird.
