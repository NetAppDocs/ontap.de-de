---
permalink: data-protection/restore-single-file-lun-task.html 
sidebar: sidebar 
keywords: restore, single file, lun, nvme, namespace, snapmirror, destination, snapshot, copy, snap mirror, baseline, 
summary: Sie können eine einzelne Datei, LUN, eine Gruppe von Dateien oder LUNs aus einem Snapshot oder einen NVMe-Namensraum von einem SnapMirror Ziel-Volume wiederherstellen. Ab ONTAP 9.7 ist auch die Wiederherstellung von NVMe-Namensräumen von einem SnapMirror synchron Ziel möglich. Dateien können auf dem ursprünglichen Quell-Volume oder auf einem anderen Volume wiederhergestellt werden. 
---
= Wiederherstellung einer Datei, LUN oder eines NVMe-Namensraums von einem ONTAP SnapMirror Ziel
:allow-uri-read: 
:icons: font
:imagesdir: ../media/


[role="lead"]
Sie können eine einzelne Datei, LUN, eine Gruppe von Dateien oder LUNs aus einem Snapshot oder einen NVMe-Namensraum von einem SnapMirror Ziel-Volume wiederherstellen. Ab ONTAP 9.7 ist auch die Wiederherstellung von NVMe-Namensräumen von einem SnapMirror synchron Ziel möglich. Dateien können auf dem ursprünglichen Quell-Volume oder auf einem anderen Volume wiederhergestellt werden.

.Bevor Sie beginnen
Um eine Datei oder LUN von einem SnapMirror synchronen Ziel wiederherzustellen (unterstützt ab ONTAP 9.5), muss zuerst die Beziehung gelöscht und freigegeben werden.

.Über diese Aufgabe
Das Volume, auf dem Sie Dateien oder LUNs wiederherstellen (das Zielvolume), muss ein Lese-/Schreibvolume sein:

* SnapMirror führt eine _inkrementelle Wiederherstellung_ durch, wenn Quell- und Zielvolume einen gemeinsamen Snapshot haben (was typischerweise der Fall ist, wenn auf das ursprüngliche Quellvolume wiederhergestellt wird).
* Andernfalls führt SnapMirror eine _Basistransfer-Wiederherstellung_ durch, bei der der angegebene Snapshot und alle von ihm referenzierten Datenblöcke auf das Ziel-Volume übertragen werden.


.Schritte
. Die Snapshots im Ziel-Volume auflisten:
+
`volume snapshot show -vserver <SVM> -volume volume`

+
Weitere Informationen zu  `volume snapshot show` finden sich in der link:https://docs.netapp.com/us-en/ontap-cli/volume-snapshot-show.html["ONTAP-Befehlsreferenz"^].

+
Das folgende Beispiel zeigt die Momentaufnahmen auf dem  `vserverB:secondary1` Ziel-Cluster:

+
[listing]
----

cluster_dst::> volume snapshot show -vserver vserverB -volume secondary1

Vserver     Volume      Snapshot                State    Size  Total% Used%
-------     ------      ---------- ----------- ------   -----  ------ -----
vserverB    secondary1  hourly.2013-01-25_0005  valid   224KB     0%    0%
                        daily.2013-01-25_0010   valid   92KB      0%    0%
                        hourly.2013-01-25_0105  valid   228KB     0%    0%
                        hourly.2013-01-25_0205  valid   236KB     0%    0%
                        hourly.2013-01-25_0305  valid   244KB     0%    0%
                        hourly.2013-01-25_0405  valid   244KB     0%    0%
                        hourly.2013-01-25_0505  valid   244KB     0%    0%

7 entries were displayed.
----
. Eine einzelne Datei oder LUN oder ein Satz von Dateien oder LUNs aus einem Snapshot in einem SnapMirror Ziel-Volume wiederherstellen:
+
`snapmirror restore -source-path <SVM:volume>|<cluster://SVM/volume>, ... -destination-path <SVM:volume>|<cluster://SVM/volume>, ... -source-snapshot snapshot -file-list <source_file_path,@destination_file_path>`

+
[NOTE]
====
Dieser Befehl muss von der Ziel-SVM oder dem Ziel-Cluster ausgeführt werden.

====
+
Der folgende Befehl stellt die Dateien  `file1` und  `file2` aus dem Snapshot  `daily.2013-01-25_0010` im ursprünglichen Zielvolume  `secondary1` an derselben Stelle im aktiven Dateisystem des ursprünglichen Quellvolumes  `primary1` wieder her:

+
[listing]
----

cluster_dst::> snapmirror restore -source-path vserverB:secondary1 -destination-path vserverA:primary1 -source-snapshot daily.2013-01-25_0010 -file-list /dir1/file1,/dir2/file2

[Job 3479] Job is queued: snapmirror restore for the relationship with destination vserverA:primary1
----
+
Der folgende Befehl stellt die Dateien  `file1` und  `file2` aus dem Snapshot  `daily.2013-01-25_0010` im ursprünglichen Zielvolume  `secondary1` an einem anderen Ort im aktiven Dateisystem des ursprünglichen Quellvolumes  `primary1` wieder her.

+
Der Zieldateipfad beginnt mit dem @-Symbol, gefolgt vom Pfad der Datei ausgehend vom Stammverzeichnis des ursprünglichen Quell-Volumes. In diesem Beispiel wird  `file1` auf  `/dir1/file1.new` wiederhergestellt und file2 auf  `/dir2.new/file2` auf  `primary1` wiederhergestellt:

+
[listing]
----

cluster_dst::> snapmirror restore -source-path vserverB:secondary1 -destination-path vserverA:primary1 -source-snapshot daily.2013-01-25_0010 -file-list /dir/file1,@/dir1/file1.new,/dir2/file2,@/dir2.new/file2

[Job 3479] Job is queued: snapmirror restore for the relationship with destination vserverA:primary1
----
+
Der folgende Befehl stellt die Dateien  `file1` und  `file3` aus dem Snapshot  `daily.2013-01-25_0010` im ursprünglichen Zielvolume  `secondary1` an verschiedenen Orten im aktiven Dateisystem des ursprünglichen Quellvolumes  `primary1` wieder her und stellt  `file2` von  `snap1` am selben Ort im aktiven Dateisystem von  `primary1` wieder her.

+
In diesem Beispiel wird die Datei `file1` wiederhergestellt zu `/dir1/file1.new` und `file3` wird wiederhergestellt zu `/dir3.new/file3`:

+
[listing]
----

cluster_dst::> snapmirror restore -source-path vserverB:secondary1 -destination-path vserverA:primary1 -source-snapshot daily.2013-01-25_0010 -file-list /dir/file1,@/dir1/file1.new,/dir2/file2,/dir3/file3,@/dir3.new/file3

[Job 3479] Job is queued: snapmirror restore for the relationship with destination vserverA:primary1
----


.Verwandte Informationen
* link:https://docs.netapp.com/us-en/ontap-cli/snapmirror-restore.html["SnapMirror Restore"^]

