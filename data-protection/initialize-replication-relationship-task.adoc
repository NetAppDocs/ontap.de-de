---
permalink: data-protection/initialize-replication-relationship-task.html 
sidebar: sidebar 
keywords: initialize, replication, relationship, snapmirror, snap mirror, synchronous, transfer, snapshot copy, 
summary: Bei allen Beziehungstypen führt die Initialisierung einen Basistransfer durch. Dabei wird eine Momentaufnahme des Quellvolumes erstellt und diese Kopie zusammen mit allen darin referenzierten Datenblöcken auf das Zielvolume übertragen. Andernfalls hängt der Inhalt der Übertragung von der Richtlinie ab. 
---
= Initialisierung einer ONTAP SnapMirror Replikationsbeziehung
:allow-uri-read: 
:icons: font
:imagesdir: ../media/


[role="lead"]
Bei allen Beziehungstypen führt die Initialisierung eine Basisübertragung durch: Dabei wird ein Snapshot des Quell-Volumes erstellt und anschließend diese Kopie zusammen mit allen darin referenzierten Datenblöcken auf das Ziel-Volume übertragen. Andernfalls hängt der Inhalt der Übertragung von der Richtlinie ab.

.Bevor Sie beginnen
Die Quell- und Ziel-Cluster sowie die SVMs müssen miteinander verbunden sein.

link:../peering/index.html["Cluster- und SVM-Peering"]

.Über diese Aufgabe
Die Initialisierung kann zeitaufwändig sein. Es empfiehlt sich, den Basistransfer außerhalb der Spitzenzeiten durchzuführen.

Ab ONTAP 9.5 werden SnapMirror synchron Beziehungen unterstützt.

Es ist zu beachten, dass die Initialisierung nicht automatisch fortgesetzt wird, wenn ein Dateisystem aus irgendeinem Grund, wie etwa einem Neustart des Knotens, einer Übernahme/Rückgabe oder einem Systemabsturz, neu gestartet wird. In diesem Fall muss die Initialisierung manuell neu gestartet werden.

.Schritt
. Eine Replikationsbeziehung initialisieren:
+
`snapmirror initialize -source-path <SVM:volume>|<cluster://SVM/volume>, ... -destination-path <SVM:volume>|<cluster://SVM/volume>, ...`

+
[NOTE]
====
Dieser Befehl muss von der Ziel-SVM oder dem Ziel-Cluster ausgeführt werden.

====
+
Das folgende Beispiel initialisiert die Beziehung zwischen dem Quellvolume `volA` auf `svm1` und dem Zielvolume `volA_dst` auf `svm_backup`:

+
[listing]
----
cluster_dst::> snapmirror initialize -source-path svm1:volA -destination-path svm_backup:volA_dst
----
+
Weitere Informationen zu  `snapmirror initialize` finden sich in der link:https://docs.netapp.com/us-en/ontap-cli/snapmirror-initialize.html["ONTAP-Befehlsreferenz"^].


