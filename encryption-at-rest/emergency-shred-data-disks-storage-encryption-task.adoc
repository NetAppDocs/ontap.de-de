---
permalink: encryption-at-rest/emergency-shred-data-disks-storage-encryption-task.html 
sidebar: sidebar 
keywords: sanitize, fips, drive, sed, psid, physical secure ID, nrd plus, emergency 
summary: Im Falle eines Sicherheitsnotfalls kann der Zugriff auf ein FIPS-Laufwerk oder eine SED sofort verhindert werden, selbst wenn das Speichersystem oder der KMIP-Server nicht mit Strom versorgt wird. 
---
= Notfall-Datenvernichtung auf einem FIPS-Laufwerk oder SED in ONTAP
:allow-uri-read: 
:icons: font
:imagesdir: ../media/


[role="lead"]
Im Falle eines Sicherheitsnotfalls kann der Zugriff auf ein FIPS-Laufwerk oder eine SED sofort verhindert werden, selbst wenn das Speichersystem oder der KMIP-Server nicht mit Strom versorgt wird.

.Bevor Sie beginnen
* Wenn ein KMIP-Server ohne verfügbare Stromversorgung verwendet wird, muss der KMIP-Server mit einem leicht zu zerstörenden Authentifizierungsobjekt (zum Beispiel einer Smartcard oder einem USB-Laufwerk) konfiguriert werden.
* Sie müssen Cluster-Administrator sein, um diese Aufgabe ausführen zu können.


.Schritt
. Notfallvernichtung von Daten auf einem FIPS-Laufwerk oder SED durchführen:
+
|===


| Wenn... 2+| Dann... 


 a| 
Die Stromversorgung des Speichersystems ist gewährleistet und es besteht ausreichend Zeit, das Speichersystem ordnungsgemäß offline zu nehmen.
 a| 
.. Wenn das Speichersystem als HA-Paar konfiguriert ist, sollte die Übernahme deaktiviert werden.
.. Alle Aggregate offline nehmen und löschen.
.. Die Berechtigungsstufe wird auf „Erweitert“ gesetzt: +
`set -privilege advanced`
.. Wenn sich das Laufwerk im FIPS-Konformitätsmodus befindet, wird die FIPS-Authentifizierungsschlüssel-ID für den Knoten wieder auf die Standard-MSID gesetzt: +
`storage encryption disk modify -disk * -fips-key-id 0x0`
.. Das Speichersystem anhalten.
.. Im Wartungsmodus starten.
.. Datenträger bereinigen oder zerstören:
+
*** Wenn die Daten auf den Festplatten unzugänglich gemacht werden sollen und die Festplatten dennoch wiederverwendet werden können, empfiehlt sich eine Bereinigung der Festplatten: +
`disk encrypt sanitize -all`
*** Wenn die Daten auf den Datenträgern unzugänglich gemacht werden sollen und die Datenträger nicht aufbewahrt werden müssen, zerstören Sie die Datenträger: +
`disk encrypt destroy disk_id1 disk_id2 …`


+

NOTE: Die  `disk encrypt sanitize` und  `disk encrypt destroy` Befehle sind ausschließlich für den Wartungsmodus reserviert. Diese Befehle müssen auf jedem HA-Knoten ausgeführt werden und stehen für defekte Festplatten nicht zur Verfügung.

.. Wiederholen Sie diese Schritte für den Partnerknoten. Dadurch befindet sich das Speichersystem dauerhaft in einem deaktivierten Zustand und alle Daten werden gelöscht. Um das System erneut zu verwenden, ist eine Neukonfiguration erforderlich.




 a| 
Die Stromversorgung des Speichersystems ist gewährleistet, und die Daten müssen sofort vernichtet werden.
 a| 
.. *Wenn Sie die Daten auf den Festplatten unzugänglich machen und die Festplatten dennoch wiederverwenden möchten, bereinigen Sie die Festplatten:*
.. Wenn das Speichersystem als HA-Paar konfiguriert ist, sollte die Übernahme deaktiviert werden.
.. Stellen Sie die Berechtigungsstufe auf „Erweitert“ ein:
+
`set -privilege advanced`

.. Wenn sich das Laufwerk im FIPS-Konformitätsmodus befindet, wird die FIPS-Authentifizierungsschlüssel-ID für den Knoten wieder auf die Standard-MSID gesetzt:
+
`storage encryption disk modify -disk * -fips-key-id 0x0`

.. Festplatte bereinigen:
+
`storage encryption disk sanitize -disk * -force-all-states true`


 a| 
.. *Wenn die Daten auf den Datenträgern unzugänglich gemacht werden sollen und die Datenträger nicht aufbewahrt werden müssen, die Datenträger zerstören:*
.. Wenn das Speichersystem als HA-Paar konfiguriert ist, sollte die Übernahme deaktiviert werden.
.. Stellen Sie die Berechtigungsstufe auf „Erweitert“ ein:
+
`set -privilege advanced`

.. Zerstören Sie die Festplatten:
`storage encryption disk destroy -disk * -force-all-states true`




 a| 
Das Speichersystem stürzt ab und befindet sich dadurch in einem dauerhaft deaktivierten Zustand, wobei alle Daten gelöscht werden. Um das System erneut zu verwenden, ist eine Neukonfiguration erforderlich.



 a| 
Strom ist für den KMIP-Server verfügbar, jedoch nicht für das Speichersystem.
 a| 
.. Melden Sie sich beim KMIP Server an.
.. Alle Schlüssel, die mit den FIPS-Laufwerken oder Self-Encrypting Drives verknüpft sind und die Daten enthalten, auf die kein Zugriff mehr möglich sein soll, werden zerstört. Dadurch wird der Zugriff des Speichersystems auf die Festplattenverschlüsselungsschlüssel verhindert.




 a| 
Strom ist für den KMIP-Server oder das Speichersystem nicht verfügbar.
 a| 
Das Authentifizierungselement für den KMIP-Server (zum Beispiel die Smartcard) wird zerstört. Dadurch wird der Zugriff des Speichersystems auf die Festplattenverschlüsselungsschlüssel verhindert.

|===


.Verwandte Informationen
* link:https://docs.netapp.com/us-en/ontap-cli/storage-encryption-disk-destroy.html["Speicherverschlüsselungsfestplatte zerstören"^]
* link:https://docs.netapp.com/us-en/ontap-cli/storage-encryption-disk-modify.html["Speicherverschlüsselung Festplatte ändern"^]
* link:https://docs.netapp.com/us-en/ontap-cli/storage-encryption-disk-sanitize.html["Speicherverschlüsselung Festplattenbereinigung"^]

