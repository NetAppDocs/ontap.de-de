---
permalink: error-messages/configure-ems-events-notifications-syslog-task.html 
sidebar: sidebar 
keywords: configuring, important, ems, events, forward, notifications, syslog, server 
summary: Um Benachrichtigungen über die schwerwiegendsten Ereignisse auf einem Syslog-Server zu protokollieren, ist eine Konfiguration des EMS erforderlich, damit Benachrichtigungen für Ereignisse, die auf wichtige Aktivitäten hinweisen, weitergeleitet werden. 
---
= Wichtige ONTAP EMS Ereignisse so konfigurieren, dass Benachrichtigungen an einen Syslog Server weitergeleitet werden
:allow-uri-read: 
:icons: font
:imagesdir: ../media/


[role="lead"]
Um Benachrichtigungen über die schwerwiegendsten Ereignisse auf einem Syslog-Server zu protokollieren, ist eine Konfiguration des EMS erforderlich, damit Benachrichtigungen für Ereignisse, die auf wichtige Aktivitäten hinweisen, weitergeleitet werden.

.Bevor Sie beginnen
DNS muss auf dem Cluster konfiguriert sein, um den Namen des Syslog Servers aufzulösen.

.Über diese Aufgabe
Falls in Ihrer Umgebung noch kein Syslog-Server für Ereignisbenachrichtigungen vorhanden ist, muss zunächst einer erstellt werden. Falls in Ihrer Umgebung bereits ein Syslog-Server zur Protokollierung von Ereignissen anderer Systeme existiert, kann dieser für wichtige Ereignisbenachrichtigungen verwendet werden.

Diese Aufgabe kann jederzeit durchgeführt werden, während der Cluster läuft, indem die Befehle in der ONTAP CLI eingegeben werden.

Ab ONTAP 9.12.1 können EMS-Ereignisse über das Transport Layer Security (TLS)-Protokoll an einen festgelegten Port eines entfernten Syslog-Servers gesendet werden. Zwei neue Parameter stehen zur Verfügung:

`tcp-encrypted`:: Wenn  `tcp-encrypted` für den  `syslog-transport` angegeben ist, überprüft ONTAP die Identität des Zielhosts durch Validierung seines Zertifikats. Der Standardwert ist `udp-unencrypted`.
`syslog-port`:: Der Standardwert `syslog-port` des Parameters hängt von der Einstellung des `syslog-transport` Parameters ab. Wenn `syslog-transport` auf `tcp-encrypted` gesetzt ist, hat `syslog-port` den Standardwert 6514.


.Schritte
. Ein Syslog-Serverziel für wichtige Ereignisse erstellen:
+
`event notification destination create -name _syslog-ems_ -syslog _syslog-server-address_ -syslog-transport {udp-unencrypted|tcp-unencrypted|tcp-encrypted}`

+
Ab ONTAP 9.12.1 können für  `syslog-transport` die folgenden Werte angegeben werden:

+
** `udp-unencrypted` - Benutzerdatengrammprotokoll ohne Sicherheit
** `tcp-unencrypted` - Transmission Control Protocol ohne Sicherheitsvorkehrungen
** `tcp-encrypted` - Transmission Control Protocol mit Transport Layer Security (TLS)
+
Das Standardprotokoll ist  `udp-unencrypted`.



+
Weitere Informationen zu  `event notification destination create` finden sich in der link:https://docs.netapp.com/us-en/ontap-cli/event-notification-destination-create.html["ONTAP-Befehlsreferenz"^].

. Die wichtigen Ereignisse werden so konfiguriert, dass Benachrichtigungen an den Syslog-Server weitergeleitet werden:
+
`event notification create -filter-name _important-events_ -destinations _syslog-ems_`

+
Weitere Informationen zu  `event notification create` finden sich in der link:https://docs.netapp.com/us-en/ontap-cli/event-notification-create.html["ONTAP-Befehlsreferenz"^].


