---
permalink: fabricpool/commands-manage-aggregates-reference.html 
sidebar: sidebar 
keywords: commands, managing, aggregates, storage aggregate object-store config 
summary: Sie verwenden die Befehle storage aggregate object-store, um Objektspeicher für FabricPool zu verwalten. Sie verwenden die Befehle storage aggregate, um Aggregate für FabricPool zu verwalten. Sie verwenden die Befehle volume, um Volumen für FabricPool zu verwalten. 
---
= ONTAP-Befehle für die Verwaltung von FabricPool-Ressourcen
:allow-uri-read: 
:icons: font
:imagesdir: ../media/


[role="lead"]
Sie verwenden die  `storage aggregate object-store` Befehle zur Verwaltung von Objektspeichern für FabricPool. Sie verwenden die  `storage aggregate` Befehle zur Verwaltung von Aggregaten für FabricPool. Sie verwenden die  `volume` Befehle zur Verwaltung von Volumes für FabricPool.

|===


| Wenn Sie möchten... | Dazu wird folgender Befehl verwendet: 


 a| 
Die Konfiguration für einen Objektspeicher wird definiert, sodass ONTAP darauf zugreifen kann
 a| 
`storage aggregate object-store config create`



 a| 
Objektspeicher-Konfigurationsattribute ändern
 a| 
`storage aggregate object-store config modify`



 a| 
Eine vorhandene Objektspeicher-Konfiguration umbenennen
 a| 
`storage aggregate object-store config rename`



 a| 
Konfiguration eines Objektspeichers löschen
 a| 
`storage aggregate object-store config delete`



 a| 
Eine Liste der Objektspeicherkonfigurationen anzeigen
 a| 
`storage aggregate object-store config show`



 a| 
Einen zweiten Objektspeicher als Spiegel an eine neue oder bestehende FabricPool anhängen
 a| 
`storage aggregate object-store mirror` mit dem  `-aggregate` und  `-name` Parameter in der Administratorberechtigungsstufe



 a| 
Einen Objektspeicher-Spiegel aus einer vorhandenen FabricPool Spiegelung entfernen
 a| 
`storage aggregate object-store unmirror` mit dem  `-aggregate` und  `-name` Parameter in der Administratorberechtigungsstufe



 a| 
Status der Spiegelungs-Resynchronisierung von FabricPool überwachen
 a| 
`storage aggregate object-store show-resync-status`



 a| 
Details zum FabricPool-Spiegel anzeigen
 a| 
`storage aggregate object-store show`



 a| 
Eine Objektspeicher-Spiegelung kann verwendet werden, um einen primären Objektspeicher in einer FabricPool Spiegelkonfiguration zu ersetzen.
 a| 
`storage aggregate object-store modify` mit dem  `-aggregate` Parameter auf der Administrator-Berechtigungsstufe



 a| 
Die Latenz und Leistung eines Objektspeichers können getestet werden, ohne den Objektspeicher an ein Aggregat anzubinden.
 a| 
`storage aggregate object-store profiler start` mit dem  `-object-store-name` und  `-node` Parameter in der erweiterten Berechtigungsstufe



 a| 
Status des Objektspeicherprofilers überwachen
 a| 
`storage aggregate object-store profiler show` mit dem  `-object-store-name` und  `-node` Parameter in der erweiterten Berechtigungsstufe



 a| 
Den Objektspeicher-Profiler abbrechen, wenn er ausgeführt wird
 a| 
`storage aggregate object-store profiler abort` mit dem  `-object-store-name` und  `-node` Parameter in der erweiterten Berechtigungsstufe



 a| 
Einen Objektspeicher an ein Aggregat anbinden, um FabricPool zu verwenden
 a| 
`storage aggregate object-store attach`



 a| 
Einen Objektspeicher an ein Aggregat anhängen, das ein FlexGroup Volume enthält, um FabricPool zu verwenden
 a| 
`storage aggregate object-store attach` mit dem  `allow-flexgroup` `true`



 a| 
Details der Objektspeicher, die an FabricPool-aktivierte Aggregate angehängt sind, anzeigen
 a| 
`storage aggregate object-store show`



 a| 
Der vom Tiering-Scan verwendete Schwellenwert für die Aggregatauslastung wird angezeigt.
 a| 
`storage aggregate object-store show` mit dem  `-fields tiering-fullness-threshold` Parameter auf der erweiterten Berechtigungsstufe



 a| 
Anzeige der Speichernutzung der Objektspeicher, die an FabricPool-fähige Aggregate angebunden sind
 a| 
`storage aggregate object-store show-space`



 a| 
Die Berichterstattung über inaktive Daten kann für ein Aggregat aktiviert werden, das nicht für FabricPool verwendet wird
 a| 
`storage aggregate modify` mit dem  `-is-inactive-data-reporting-enabled true` Parameter



 a| 
Anzeigen, ob die Berichterstattung über inaktive Daten auf einem Aggregat aktiviert ist
 a| 
`storage aggregate show` mit dem  `-fields is-inactive-data-reporting-enabled` Parameter



 a| 
Informationen darüber, wie viele Nutzerdaten innerhalb eines Aggregats kalt sind, anzeigen
 a| 
`storage aggregate show-space` mit dem  `-fields performance-tier-inactive-user-data,performance-tier-inactive-user-data-percent` Parameter



 a| 
Ein Volume für FabricPool erstellen, einschließlich der folgenden Angaben:

* Die Tiering-Richtlinie
* Die Mindestkühlzeit für das Tiering (für die  `snapshot-only` oder  `auto` Tiering-Richtlinie)

 a| 
`volume create`

* Der  `-tiering-policy`Parameter wird verwendet, um die Tiering-Richtlinie anzugeben.
* Der  `-tiering-minimum-cooling-days` Parameter auf der erweiterten Berechtigungsstufe gibt den Mindestkühlzeitraum für das Tiering an.




 a| 
Ein Volume für FabricPool kann geändert werden, einschließlich der folgenden Anpassungen:

* Die Tiering-Richtlinie
* Die Mindestkühlzeit für das Tiering (für die  `snapshot-only` oder  `auto` Tiering-Richtlinie)

 a| 
`volume modify`

* Der  `-tiering-policy`Parameter wird verwendet, um die Tiering-Richtlinie anzugeben.
* Der  `-tiering-minimum-cooling-days` Parameter auf der erweiterten Berechtigungsstufe gibt den Mindestkühlzeitraum für das Tiering an.




 a| 
Informationen zu FabricPool in Bezug auf ein Volumen, einschließlich der folgenden:

* Die Mindestkühlperiode für das Tiering
* Wie viele Nutzerdaten sind kalt

 a| 
`volume show`

* Sie verwenden den  `-fields tiering-minimum-cooling-days` Parameter in der erweiterten Berechtigungsstufe, um die Mindestkühlzeit für das Tiering anzuzeigen.
* Der  `-fields performance-tier-inactive-user-data,performance-tier-inactive-user-data-percent`Parameter zeigt an, wie viele Benutzerdaten kalt sind.




 a| 
Ein Volume in oder aus FabricPool verschieben
 a| 
`volume move start`Der  `-tiering-policy`optionale Parameter gibt die Tiering-Richtlinie für das Volume an.



 a| 
Den Schwellenwert für die Rückgewinnung von nicht referenziertem Speicherplatz (den Defragmentierungsschwellenwert) für FabricPool anpassen
 a| 
`storage aggregate object-store modify` mit dem  `-unreclaimed-space-threshold` Parameter auf der erweiterten Berechtigungsstufe



 a| 
Den Schwellenwert für den Prozentsatz festlegen, bei dem das Aggregat als voll gilt, bevor der Tiering-Scan mit dem Tiering von Daten für FabricPool beginnt

FabricPool lagert weiterhin kalte Daten in eine Cloud-Tier aus, bis die lokale Tier 98 % Kapazität erreicht.
 a| 
`storage aggregate object-store modify` mit dem  `-tiering-fullness-threshold` Parameter auf der erweiterten Berechtigungsstufe



 a| 
Schwellenwert für die Rückgewinnung von nicht referenziertem Speicherplatz für FabricPool anzeigen
 a| 
`storage aggregate object-store show` oder  `storage aggregate object-store show-space` Befehl mit dem  `-unreclaimed-space-threshold` Parameter in der erweiterten Berechtigungsstufe

|===
.Verwandte Informationen
* link:https://docs.netapp.com/us-en/ontap-cli/storage-aggregate-modify.html["storage aggregate ändern"^]
* link:https://docs.netapp.com/us-en/ontap-cli/search.html?q=storage+aggregate+object["Speicher-Aggregatobjekt"^]
* link:https://docs.netapp.com/us-en/ontap-cli/storage-aggregate-show-space.html["storage aggregate show-space"^]

