---
permalink: san-admin/move-san-lifs-task.html 
sidebar: sidebar 
keywords: move, san, lifs 
summary: Wenn ein Knoten offline genommen werden muss, können Sie eine SAN-LIF verschieben, um deren Konfigurationsinformationen, wie z. B. die WWPN, zu erhalten und eine Neuzonierung der Switch-Fabric zu vermeiden. 
---
= Verschieben Sie eine ONTAP SAN LIF
:allow-uri-read: 
:icons: font
:imagesdir: ../media/


[role="lead"]
Wenn ein Knoten offline genommen werden muss, können Sie eine SAN LIF verschieben, um deren Konfigurationsinformationen, wie z. B. die WWPN, zu erhalten. Das Verschieben einer LIF erfordert weder eine Neuzonierung Ihres FC-Fabric noch die Erstellung neuer iSCSI-Sitzungen zwischen Cluster-Hosts und der Ziel-Schnittstelle. Sie können SAN LIFs auf jeden beliebigen Knoten im Cluster verschieben, jedoch nicht zwischen Storage Virtual Machines (SVMs).

Sie können eine LIF verschieben, um die folgenden Aktionen unterbrechungsfrei durchzuführen:

* Ersetzen Sie ein HA-Paar eines Clusters durch ein aktualisiertes HA-Paar auf eine Weise, die für Hosts, die auf LUN- oder Namespace-Daten zugreifen, transparent ist
* Upgrade einer Ziel-Interfacekarte
* Die Ressourcen einer Storage Virtual Machine (SVM) von einer Gruppe von Knoten in einem Cluster auf eine andere Gruppe von Knoten im Cluster verschieben


Sie können ein SAN LIF nicht mit dem  `network interface move` Befehl verschieben. Um ein SAN LIF zu verschieben, müssen Sie es offline nehmen, auf einen neuen Home-Node oder Port verschieben und dann wieder online schalten. Da ein SAN LIF vor der Verschiebung offline genommen werden muss, muss der Host-Datenverkehr auf Host-Multipathing-Software zurückgreifen, um unterbrechungsfreien Zugriff auf die LUN zu gewährleisten, sodass keine E/A-Unterbrechung auftritt.

.Bevor Sie beginnen
Wenn die LIF Mitglied eines Portsets ist, muss die LIF aus dem Portset entfernt worden sein, bevor die LIF auf einen anderen Knoten verschoben werden kann.

.Über diese Aufgabe
Der Zielknoten und der physische Port einer LIF, die Sie verschieben möchten, müssen sich im selben FC-Fabric oder Ethernet-Netzwerk befinden. Wenn eine LIF in ein anderes Fabric verschoben wird, das nicht korrekt zoniert ist, oder wenn eine LIF in ein Ethernet-Netzwerk verschoben wird, das keine Verbindung zwischen iSCSI-Initiator und Ziel aufweist, ist die LUN nach der Wiederinbetriebnahme nicht erreichbar.

.Schritte
. Der administrative und operative Status des LIF wird angezeigt:
+
[source, cli]
----
network interface show -vserver <SVM_name>
----
+
Weitere Informationen zu  `network interface show` finden sich in der link:https://docs.netapp.com/us-en/ontap-cli/network-interface-show.html["ONTAP-Befehlsreferenz"^].

. Den Status des LIF auf  `down` (offline) ändern:
+
[source, cli]
----
network interface modify -vserver <SVM_name> -lif <LIF_name> -status-admin down
----
+
Weitere Informationen zu  `network interface modify` finden sich in der link:https://docs.netapp.com/us-en/ontap-cli/network-interface-modify.html["ONTAP-Befehlsreferenz"^].

. Dem LIF werden ein neuer Knoten und ein neuer Port zugewiesen:
+
[source, cli]
----
network interface modify -vserver <SVM_name> -lif <LIF_name> -home-node <node_name> -home-port <port_name>
----
. Den Status des LIF auf  `up` (online) ändern:
+
[source, cli]
----
network interface modify -vserver <SVM_name> -lif <LIF_name> -status-admin up
----
. Änderungen überprüfen:
+
[source, cli]
----
network interface show -vserver <SVM_name>
----

